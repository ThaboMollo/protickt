# The Express API base URL (no trailing slash). Local dev defaults to
# http://localhost:4000; production defaults to https://protickt-api.vercel.app.
# Set this in Vercel if you attach a custom API domain.
NEXT_PUBLIC_API_URL=http://localhost:4000
